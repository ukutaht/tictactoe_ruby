#!/usr/bin/env ruby

require_relative '../lib/tic_tac_toe'
game = TicTacToe::Core::Game.new
controllers = [TicTacToe::GUI::SetupController.new(game), TicTacToe::GUI::GameplayController.new(game, 200)]

TicTacToe::GUIDisplay.new(controllers, 600).show
